version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10
    branches:
      only:
        - master
    steps:
      - add_ssh_keys:
          fingerprints:
            - "00:e4:d5:9c:c4:bb:83:c6:07:3e:53:9f:3d:30:3a:a3"
      - checkout
      - run:
        name: Install
        command: yarn install
      - run:
        name: Build
        command: yarn build
